"""Quiz requirement helper.

This file exists to satisfy the submission rubric asking for `requirements.py`.

Source of truth for installing dependencies remains `requirements.txt`:

    pip install -r requirements.txt

"""

REQUIREMENTS = [
    "Django>=5.0,<6.0",
    "djangorestframework>=3.15,<4.0",
    "django-cors-headers>=4.4,<5.0",
    "django-environ>=0.11,<1.0",
]
